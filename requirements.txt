# Core dependencies
torch>=2.0.0
transformers>=4.35.0
peft>=0.6.0
datasets>=2.14.0
accelerate>=0.24.0
safetensors>=0.4.0

# Optional but recommended
bitsandbytes>=0.41.0  # For 4-bit quantization support
wandb>=0.15.0  # For experiment tracking

# Utilities
tqdm>=4.65.0
pyyaml>=6.0  # or ruamel.yaml>=0.18.0
numpy>=1.24.0
scipy>=1.10.0  # For scientific computing
pandas>=2.0.0  # For data processing
